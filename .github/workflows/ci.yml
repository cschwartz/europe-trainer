name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      # the npm bundled with setup-node's Node 22 mishandles optional
      # platform-specific deps in lockfileVersion 3 lockfiles (EBADPLATFORM)
      - run: npm install -g npm@11
      - run: npm ci
      - run: npm run typecheck
      - run: npm run validate
      - run: npm run test:unit
      - run: npx playwright install --with-deps chromium
      - run: npm run build
      - run: npm run test:e2e
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
      - if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: test-results/
          retention-days: 7
